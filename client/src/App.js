import React from 'react'; // Import React so we can use JSX

// Create a functional component called App
function App() {
  // JSX that will be rendered on the screen
  return <h1>🍕 Welcome to Pizza Delivery App</h1>;
}

// Export the component so it can be used in other files (like index.js)
export default App;
